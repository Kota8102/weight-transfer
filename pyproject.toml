[project]
name = "model-diff"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.9.0",
    "torch>=2.7.1",
    "transformers>=4.53.3",
]

[dependency-groups]
dev = [
    "ruff>=0.12.4",
]
